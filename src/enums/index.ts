export enum RolesLogin {
  CUSTOMER = "CUSTOMER",
  STORE = "STORE",
  ADMIN = "ADMIN",
}

export enum Roles {
  CUSTOMER = "Khách hàng",
  STORE = "Cửa hàng",
  ADMIN = "QTV",
}

export enum CreateRoles {
  CUSTOMER = 0,
  STORE = 1,
  ADMIN = 2,
}
